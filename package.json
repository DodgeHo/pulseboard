{
  "name": "pulseboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "compose:up": "docker compose up --build",
    "compose:down": "docker compose down",
    "compose:e2e": "bash scripts/local-compose-e2e.sh",
    "compose:integration": "bash scripts/local-compose-integration.sh",
    "dev:api": "corepack pnpm --filter @pulseboard/api dev",
    "dev:worker": "corepack pnpm --filter @pulseboard/worker dev",
    "db:generate": "corepack pnpm --filter @pulseboard/db db:generate",
    "db:push": "corepack pnpm --filter @pulseboard/db db:push",
    "db:deploy": "corepack pnpm --filter @pulseboard/db db:deploy",
    "db:seed": "corepack pnpm --filter @pulseboard/db db:seed",
    "demo:flow": "tsx scripts/demo-flow.ts",
    "doctor": "tsx scripts/doctor.ts",
    "typecheck": "tsc --noEmit -p tsconfig.tools.json && corepack pnpm -r typecheck",
    "test": "corepack pnpm -r test",
    "test:integration": "corepack pnpm --filter @pulseboard/api test:integration",
    "lint": "corepack pnpm typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "prisma": "^6.19.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
