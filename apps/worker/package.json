{
  "name": "@pulseboard/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pulseboard/core": "workspace:*",
    "@pulseboard/db": "workspace:*",
    "@pulseboard/queues": "workspace:*",
    "bullmq": "^5.60.0",
    "pino": "^9.7.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}

